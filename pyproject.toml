[project]
name = "ai-router"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "httpx",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
