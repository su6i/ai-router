# API Versioning Fixture

## Semantic Versioning

Bump the major version for breaking changes, the minor version for
backward-compatible additions, and the patch version for bug fixes only.

## Deprecation Window

Announce a deprecated endpoint at least one full release ahead of removing
it, keeping the old response shape available behind a feature flag.
