# Logging Fixture

## Log Levels

Use structured logging with levels DEBUG, INFO, WARNING, and ERROR. Never
print secrets or credentials to any log stream.

## Retention

Rotate log files on a schedule and compress anything past its retention
window.
